{
  "name": "usg-meter-sizing",
  "version": "1.0.0",
  "private": true,
  "description": "Holland Supply meter sizing algorithm. The browser bundle in dist/ is generated; see README.md.",
  "main": "dist/usg-meter-sizing.js",
  "scripts": {
    "build": "python3 tools/build.py",
    "test": "python3 tests/test_parity.py && node tests/test_block.js && node tests/test_pdf.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "jspdf": "^2.5.1"
  }
}
